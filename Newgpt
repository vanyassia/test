<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>MultiTool — современный веб‑приложение</title>
  <!-- Шрифты и иконки -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <!-- AOS для анимаций при скролле -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet"/>

  <style>
    /* Сброс и базовые стили */
    *, *::before, *::after { box-sizing: border-box; margin:0; padding:0; }
    html { scroll-behavior: smooth; }
    body {
      font-family: 'Inter', -apple-system, system-ui, sans-serif;
      background: #f0f2f5; color: #1c1e21;
      display: flex; flex-direction: column; min-height: 100vh;
    }
    a { text-decoration: none; color: inherit; }
    button { cursor: pointer; }

    /* Переменные и общие */
    :root {
      --primary: #0a66c2;
      --bg: #ffffff;
      --bg-alt: #f7f9fa;
      --text: #1c1e21;
      --muted: #65676b;
      --shadow: rgba(0,0,0,0.05);
      --radius: 8px;
      --gap: 1.5rem;
      --transition: 0.3s;
    }
    .container { width: 90%; max-width: 1200px; margin: auto; }

    /* Header */
    header {
      position: sticky; top: 0; z-index: 100;
      background: var(--bg); box-shadow: 0 2px 4px var(--shadow);
    }
    .nav-bar {
      display: flex; align-items: center; justify-content: space-between;
      padding: 1rem 0;
    }
    .logo { font-size: 1.5rem; font-weight: 600; color: var(--primary); }
    nav.desktop { display: flex; gap: var(--gap); }
    nav.desktop a {
      padding: 0.5rem 0.75rem; border-radius: var(--radius);
      font-weight: 500; transition: background var(--transition);
    }
    nav.desktop a:hover, nav.desktop a.active {
      background: var(--bg-alt);
    }
    .burger { display: none; flex-direction: column; gap: 4px; }
    .burger div {
      width: 25px; height: 3px; background: var(--text);
      transition: transform var(--transition), opacity var(--transition);
    }
    @media (max-width: 768px) {
      nav.desktop { display: none; }
      .burger { display: flex; }
    }
    nav.mobile {
      display: none; flex-direction: column; gap: 1rem;
      background: var(--bg); padding: 1rem; box-shadow: 0 4px 8px var(--shadow);
    }
    nav.mobile.show { display: flex; }

    /* Hero */
    .hero {
      position: relative;
      background: url('https://source.unsplash.com/1600x900/?technology') center/cover no-repeat;
      color: #fff; text-align: center;
      padding: 6rem 1rem;
    }
    .hero::after {
      content: ''; position: absolute; inset:0; background:rgba(0,0,0,0.5);
    }
    .hero .container { position: relative; z-index: 1; }
    .hero h1 { font-size: 3rem; margin-bottom: 1rem; animation: fadeInDown 1s; }
    .hero p  { font-size: 1.125rem; animation: fadeInUp 1s; }
    @keyframes fadeInDown {
      from { opacity:0; transform: translateY(-20px) } to { opacity:1; transform: translateY(0) }
    }
    @keyframes fadeInUp {
      from { opacity:0; transform: translateY(20px) } to { opacity:1; transform: translateY(0) }
    }

    /* Сетка функций */
    .features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: var(--gap);
      margin: 3rem 0;
    }
    .card {
      background: var(--bg); border-radius: var(--radius);
      box-shadow: 0 2px 6px var(--shadow);
      padding: 1.5rem; text-align: center;
      transition: transform var(--transition), box-shadow var(--transition);
    }
    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 6px 16px var(--shadow);
    }
    .card i {
      font-size: 2rem; color: var(--primary);
      margin-bottom: 0.75rem;
      transition: transform var(--transition);
    }
    .card:hover i { transform: scale(1.1); }
    .card h3 { margin-bottom: 0.5rem; }
    .card p  { color: var(--muted); font-size: 0.95rem; }
    .card button {
      margin-top: 1rem; padding: 0.6rem 1.2rem;
      border: none; background: var(--primary); color: #fff;
      border-radius: var(--radius); transition: background var(--transition), transform .1s;
    }
    .card button:active { transform: scale(0.97); }

    /* Разделы с формами */
    section.form-section {
      margin-bottom: 4rem;
    }
    section.form-section h2 {
      text-align: center; color: var(--primary);
      margin-bottom: 2rem; position: relative;
    }
    section.form-section h2::after {
      content: ''; display: block;
      width: 60px; height: 3px; background: var(--primary);
      margin: 0.5rem auto 0;
    }
    .form-card {
      max-width: 600px; margin: auto;
      background: var(--bg); border-radius: var(--radius);
      box-shadow: 0 2px 6px var(--shadow);
      padding: 2rem;
      opacity: 0; transform: translateY(20px);
      transition: opacity .6s ease, transform .6s ease;
    }
    .form-card.aos-animate {
      opacity: 1; transform: translateY(0);
    }
    .form-card label {
      display: block; margin-top: 1rem;
      color: var(--text);
    }
    .form-card input, .form-card textarea {
      width: 100%; padding: 0.75rem; margin-top: 0.25rem;
      border: 1px solid #ccc; border-radius: var(--radius);
      font-size: 1rem; transition: border-color var(--transition);
    }
    .form-card input:focus, .form-card textarea:focus {
      border-color: var(--primary);
    }
    .form-card button {
      margin-top: 1.5rem; width: 100%;
      padding: 0.75rem; font-size: 1rem;
      border: none; background: var(--primary);
      color: #fff; border-radius: var(--radius);
      transition: background var(--transition), transform .1s;
    }
    .form-card button:active { transform: scale(0.97); }
    .form-card .result {
      margin-top: 1rem; font-weight: 500;
    }

    /* Footer */
    footer {
      text-align: center; padding: 1.5rem 0;
      background: var(--bg-alt); color: var(--muted);
      font-size: 0.9rem; margin-top: auto;
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <div class="container nav-bar">
      <div class="logo">MultiTool</div>
      <nav class="desktop" id="mainNav">
        <a href="#calculator" class="active">Калькулятор</a>
        <a href="#battleship">Морской бой</a>
        <a href="#guess">Угадайка</a>
        <a href="#currency">Курс валют</a>
        <a href="#askai">Нейросеть</a>
        <a href="#magic">8 шар</a>
        <a href="#coins">Монетки</a>
        <a href="#fib">Фибоначчи</a>
        <a href="#grammar">Грамматика</a>
        <a href="#baseconv">Системы</a>
        <a href="#spellcheck">Проверка</a>
      </nav>
      <div class="burger" id="burger">
        <div></div><div></div><div></div>
      </div>
    </div>
    <nav class="mobile container" id="mobileNav"></nav>
  </header>

  <!-- Hero -->
  <section class="hero">
    <div class="container">
      <h1>MultiTool — всё в одном</h1>
      <p>Калькулятор, игры, нейросеть, проверки — современный веб‑инструмент под рукой.</p>
    </div>
  </section>

  <main class="container">

    <!-- Сетка функций -->
    <section class="features">
      <div class="card" data-aos="fade-up">
        <i class="fas fa-calculator"></i>
        <h3>Калькулятор</h3>
        <p>Мгновенные вычисления любых выражений.</p>
        <button onclick="scrollToSection('calculator')">Перейти</button>
      </div>
      <div class="card" data-aos="fade-up" data-aos-delay="100">
        <i class="fas fa-ship"></i>
        <h3>Морской бой</h3>
        <p>Классика: потопи все корабли противника.</p>
        <button onclick="scrollToSection('battleship')">Перейти</button>
      </div>
      <div class="card" data-aos="fade-up" data-aos-delay="200">
        <i class="fas fa-question-circle"></i>
        <h3>Угадайка</h3>
        <p>Отгадай число от 1 до 100.</p>
        <button onclick="scrollToSection('guess')">Перейти</button>
      </div>
      <div class="card" data-aos="fade-up" data-aos-delay="300">
        <i class="fas fa-coins"></i>
        <h3>Монетки</h3>
        <p>Накопи свои виртуальные монетки.</p>
        <button onclick="scrollToSection('coins')">Перейти</button>
      </div>
      <!-- Добавьте ещё карточек по аналогии -->
    </section>

    <!-- Детальные секции -->
    <section id="calculator" class="form-section">
      <h2 data-aos="fade-up">Калькулятор</h2>
      <div class="form-card" data-aos="fade-up" data-aos-delay="100">
        <input id="calcExpr" placeholder="2+3*4">
        <button onclick="doCalc()">Вычислить</button>
        <div class="result" id="calcRes"></div>
      </div>
    </section>

    <section id="battleship" class="form-section">
      <h2 data-aos="fade-up">Морской бой</h2>
      <div class="form-card" data-aos="fade-up" data-aos-delay="100">
        <button onclick="startBS()">Новая игра</button>
        <div class="result" id="bsBoard"></div>
      </div>
    </section>

    <section id="guess" class="form-section">
      <h2 data-aos="fade-up">Угадайка</h2>
      <div class="form-card" data-aos="fade-up" data-aos-delay="100">
        <button onclick="startGuess()">Загадать</button>
        <input type="number" id="guessNum">
        <button onclick="checkGuess()">Проверить</button>
        <div class="result" id="guessRes"></div>
      </div>
    </section>

    <section id="coins" class="form-section">
      <h2 data-aos="fade-up">Монетки</h2>
      <div class="form-card" data-aos="fade-up" data-aos-delay="100">
        <button onclick="showCoins()">Получить монетки</button>
        <div class="result" id="coinsRes"></div>
      </div>
    </section>

    <!-- ... другие секции аналогично ... -->

  </main>

  <footer>
    © 2025 MultiTool — все права защищены
  </footer>

  <!-- Скрипты -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/mathjs/11.8.0/math.min.js"></script>
  <script>
    // Инициализация AOS
    AOS.init({ once: true, duration: 600 });

    // Клонируем меню для мобильного
    const mainNav = document.getElementById('mainNav');
    document.getElementById('mobileNav').innerHTML = mainNav.innerHTML;

    // Бургер‑меню
    document.getElementById('burger').onclick = () => {
      document.getElementById('mobileNav').classList.toggle('show');
    };

    // Подсветка активного пункта
    document.querySelectorAll('nav a').forEach(a => {
      a.addEventListener('click', e => {
        document.querySelectorAll('nav a').forEach(x => x.classList.remove('active'));
        e.target.classList.add('active');
        document.getElementById('mobileNav').classList.remove('show');
      });
    });

    // Плавный скролл
    function scrollToSection(id) {
      document.getElementById(id).scrollIntoView({ behavior: 'smooth', block: 'start' });
    }

    // Функции (пример: калькулятор и морской бой)
    function doCalc(){
      const e = document.getElementById('calcExpr').value;
      try { const r = math.evaluate(e); document.getElementById('calcRes').textContent = `${e} = ${r}`; }
      catch(err){ document.getElementById('calcRes').textContent = 'Ошибка'; }
    }

    let bsField, bsShips;
    function startBS(){
      bsField = Array.from({length:5},()=>Array(5).fill('O'));
      bsShips = new Set(); while(bsShips.size<3) bsShips.add(`${rand(5)},${rand(5)}`);
      renderBS();
    }
    function renderBS(){
      const c = document.getElementById('bsBoard'); let html='<table>';
      bsField.forEach((r,i)=>{ html+='<tr>'; r.forEach((v,j)=>{
        html+=`<td><button onclick="clickBS(${i},${j})">${v}</button></td>`;
      }); html+='</tr>'; }); html+='</table>'; c.innerHTML=html;
    }
    function clickBS(i,j){
      const k=`${i},${j}`; if(bsShips.has(k)){ bsField[i][j]='✖'; bsShips.delete(k);} else bsField[i][j]='−'; renderBS();
      if(!bsShips.size) setTimeout(()=>alert('Победа! 🎉'),100);
    }

    let secret;
    function startGuess(){ secret = rand(100)+1; document.getElementById('guessRes').textContent='Угадайте!'; }
    function checkGuess(){
      const n=+document.getElementById('guessNum').value;
      let m = n<secret?'Больше':n>secret?'Меньше':'Угадали! 🎉';
      document.getElementById('guessRes').textContent = m;
    }

    function showCoins(){
      let c=+localStorage.coins||0, d=rand(10); c+=d; localStorage.coins=c;
      document.getElementById('coinsRes').textContent=`У вас ${c} монеток (+${d})`;
    }

    function rand(n){ return Math.floor(Math.random()*n); }
  </script>
</body>
</html>
