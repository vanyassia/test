<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Coffee Shop</title>
  <!-- Пример подключения Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link 
    href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" 
    rel="stylesheet"
  >
  <style>
    /* Сброс некоторых стилей по умолчанию */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Montserrat', sans-serif;
      background-color: #f4ede7;
      color: #2c2c2c;
      line-height: 1.5;
    }

    header {
      background-color: #3a2d29; /* Тёмно-коричневый оттенок */
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    /* Логотип / Название */
    .logo {
      color: #f4ede7;
      font-size: 1.5rem;
      font-weight: 700;
      text-decoration: none;
    }

    /* Навигация */
    nav ul {
      list-style: none;
      display: flex;
      gap: 1.5rem;
    }

    nav a {
      color: #f4ede7;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: #d3c8c2;
    }

    /* Hero-секция (блок с большой картинкой и заголовком) */
    .hero {
      display: flex;
      flex-direction: row;
      flex-wrap: wrap;
      align-items: center;
      padding: 2rem;
      background-color: #e2d4c8; /* Светло-бежевый оттенок */
    }

    .hero-content {
      flex: 1 1 400px;
      padding: 1rem;
    }

    .hero-content h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      font-weight: 700;
    }

    .hero-content p {
      font-size: 1.2rem;
      margin-bottom: 1.5rem;
    }

    .hero-content .btn {
      background-color: #3a2d29;
      color: #f4ede7;
      padding: 0.75rem 1.5rem;
      border: none;
      cursor: pointer;
      font-size: 1rem;
      font-weight: 600;
      transition: background-color 0.3s ease;
    }

    .hero-content .btn:hover {
      background-color: #2c221e;
    }

    .hero-image {
      flex: 1 1 400px;
      text-align: center;
      padding: 1rem;
    }

    .hero-image img {
      max-width: 100%;
      height: auto;
      border-radius: 5px;
    }

    /* Блок "специальных предложений" и т.п. */
    .offers {
      display: flex;
      flex-wrap: wrap;
      gap: 2rem;
      padding: 2rem;
    }

    .offer-block {
      flex: 1 1 300px;
      background-color: #f4ede7;
      padding: 2rem;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      text-align: center;
    }

    .offer-block h2 {
      font-size: 1.5rem;
      margin-bottom: 1rem;
    }

    .offer-block p {
      margin-bottom: 1.5rem;
    }

    .offer-block img {
      width: 100px;
      height: auto;
      margin-bottom: 1rem;
    }

    .offer-block .btn-offer {
      display: inline-block;
      background-color: #3a2d29;
      color: #f4ede7;
      padding: 0.75rem 1.5rem;
      border: none;
      cursor: pointer;
      font-weight: 600;
      text-decoration: none;
      transition: background-color 0.3s ease;
    }

    .offer-block .btn-offer:hover {
      background-color: #2c221e;
    }

    /* Блок с картинкой интерьера */
    .interior {
      display: flex;
      padding: 2rem;
      gap: 2rem;
      flex-wrap: wrap;
      background-color: #e2d4c8;
    }

    .interior-image {
      flex: 1 1 400px;
      text-align: center;
    }

    .interior-image img {
      max-width: 100%;
      border-radius: 5px;
    }

    .interior-content {
      flex: 1 1 400px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .interior-content p {
      font-size: 1.1rem;
      line-height: 1.6;
    }

    /* Footer */
    footer {
      background-color: #3a2d29;
      color: #f4ede7;
      text-align: center;
      padding: 1rem;
    }

  </style>
</head>
<body>

  <!-- Шапка сайта (header) -->
  <header>
    <a href="#" class="logo">COFFEE SHOP</a>
    <nav>
      <ul>
        <li><a href="#home">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#menu">Menu</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <!-- Hero-секция -->
  <section class="hero" id="home">
    <div class="hero-content">
      <h1>GREAT COFFEE SHOP</h1>
      <p>We provide the best coffee in the city</p>
      <button class="btn">VIEW OUR MENU</button>
    </div>
    <div class="hero-image">
      <!-- Замените ссылку на реальную картинку -->
      <img 
        src="https://via.placeholder.com/600x400?text=Coffee+Cup" 
        alt="Coffee Cup" 
      />
    </div>
  </section>

  <!-- Блок с предложениями/скидками -->
  <section class="offers">
    <div class="offer-block">
      <!-- Замените ссылку на реальную картинку с кофейными зёрнами или пакетами -->
      <img 
        src="https://via.placeholder.com/100?text=Bags" 
        alt="Coffee Bags" 
      />
      <h2>Enjoy Our Special Offers</h2>
      <p>Get 20% off your first order today</p>
      <a href="#" class="btn-offer">Get Started</a>
    </div>
    <div class="offer-block">
      <!-- Можно добавить второе предложение или описание -->
      <img 
        src="https://via.placeholder.com/100?text=Beans" 
        alt="Coffee Beans" 
      />
      <h2>Fresh Coffee Beans</h2>
      <p>Roasted daily for the perfect taste</p>
      <a href="#" class="btn-offer">Shop Now</a>
    </div>
  </section>

  <!-- Блок с интерьером / атмосферой кофейни -->
  <section class="interior">
    <div class="interior-image">
      <!-- Замените ссылку на реальную картинку интерьера -->
      <img 
        src="https://via.placeholder.com/600x400?text=Coffee+Shop+Interior" 
        alt="Coffee Shop Interior" 
      />
    </div>
    <div class="interior-content">
      <p>
        Our cozy interior makes you feel at home. Whether you're looking for a quiet spot to work or a comfortable place to meet friends, our shop is the perfect destination.
      </p>
    </div>
  </section>

  <!-- Подвал (footer) -->
  <footer>
    <p>&copy; 2025 Coffee Shop. All rights reserved.</p>
  </footer>
</body>
</html>
